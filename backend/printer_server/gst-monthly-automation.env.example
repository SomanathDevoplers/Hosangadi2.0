# Copy this file to the per-user configuration location documented in
# GST_MONTHLY_AUTOMATION.md. Do not commit an App Password.

# Scheduled runs do nothing until this is explicitly true.
GST_MONTHLY_AUTOMATION_ENABLED=false

# Existing repository database defaults are localhost:3306, root, mysqlpassword5.
# Uncomment only when the target system needs overrides.
# GST_DB_HOST=localhost
# GST_DB_PORT=3306
# GST_DB_USER=root
# GST_DB_PASSWORD=
# GST_DB_CONNECT_TIMEOUT_MS=15000

GST_SMTP_HOST=smtp.gmail.com
GST_SMTP_PORT=465
GST_SMTP_SECURE=true
GST_SMTP_USER=somanathstoresmaravanthe@gmail.com
GST_SMTP_PASSWORD=REPLACE_WITH_GMAIL_APP_PASSWORD

GST_EMAIL_FROM=somanathstoresmaravanthe@gmail.com
GST_EMAIL_TO=jathindra_co@yahoo.com
GST_EMAIL_CC=vshivanand2@gmail.com
GST_ERROR_EMAIL_TO=vshivanand2@gmail.com

# Optional operational overrides.
# GST_REPORT_TIMEOUT_MS=900000
# GST_AUTOMATION_STATE_DIR=C:\path\to\state
# GST_DESKTOP_DIRECTORY=C:\path\to\Desktop
